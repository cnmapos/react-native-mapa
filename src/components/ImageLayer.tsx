import { View } from 'react-native';
/**
 * ImageLayer props
 *
 * @category Props
 */
export type ImageLayerProps = {};

/**
  @category Component
 */
const ImageLayer = (props: ImageLayerProps) => {
    return <View>ImageLayer</View>;
};

export default ImageLayer;
