import { View } from 'react-native';
/**
 * POIFinder props
 *
 * @category Props
 */
export type POIFinderProps = {};

/**
  @category Component
 */
const POIFinder = (props: POIFinderProps) => {
    return <View>POIFinder</View>;
};

export default POIFinder;
